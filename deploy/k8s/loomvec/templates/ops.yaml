apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "loomvec.fullname" . }}-ops
  labels:
    {{- include "loomvec.labels" . | nindent 4 }}
    app.kubernetes.io/component: ops
spec:
  replicas: {{ .Values.ops.replicaCount }}
  {{- with .Values.ops.strategy }}
  strategy:
    {{- include "loomvec.updateStrategy" . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "loomvec.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: ops
  template:
    metadata:
      labels:
        {{- include "loomvec.componentLabels" (dict "component" "ops" "context" .) | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "loomvec.serviceAccountName" . }}
      containers:
        - name: ops
          image: {{ include "loomvec.image" (dict "root" $ "image" .Values.ops.image) }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.ops.port }}
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          {{- with .Values.ops.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
      {{- with .Values.ops.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.ops.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.ops.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "loomvec.fullname" . }}-ops
  labels:
    {{- include "loomvec.labels" . | nindent 4 }}
    app.kubernetes.io/component: ops
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.ops.port }}
      targetPort: http
  selector:
    {{- include "loomvec.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: ops
