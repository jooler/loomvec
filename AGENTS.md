# LoomVec Agent 指引

本文件是跨工具共用的权威规则。Cursor / Claude Code 等入口只保留铁律并链接到此。

## 改动交付流程（每次改动都走）

顺序固定：写代码 → 同步 docs/ → 新会话审查并修完 → 提交。提交只发生在最后一步。

### 一、写代码

按项目原有的开发/测试规范。改完不提交。

### 二、同步 docs/

`docs/` 只记录当前代码的事实：系统现在是什么样、怎么工作、接口和参数是什么。

- 改了什么行为，就把对应描述改写成现状；已不存在的东西直接删，不留「已废弃/原来是……」。
- 不写进 `docs/`：修改日志（`git log` 才是权威）、方案选择逻辑（为什么选 A 不选 B）、待办、路线图、研究结论、复盘。
- 研究/规划/方案/待办/进度类文档放独立文件夹 `planning/`（与 `docs/` 平级）。
- 每条事实回代码核对（路径、函数名、参数名真实存在），不凭记忆、不照抄 diff。
- 没有文档受影响就明确说「无需改 docs」，不静默跳过。

存量混合文档（既有现状又有历史）不要一次拆完；下次改到它时再拆。迁移路径时先 grep 代码里有没有程序读写这些文档路径。

### 三、新会话审查 → 修完 → 提交

1. 写代码的会话不自审、不提交。结束时交代：改了什么、所在工作目录/分支，提示开新会话审查。
2. 审查在新开的会话里做（干净上下文）。范围 = 未提交的改动：`git status` + `git diff` + 未跟踪文件，含 `docs/` / `planning/` 的改动。
3. 审查会话必须在放着未提交改动的那份工作目录里开。多会话共用同一目录时，只审自己的改动，别碰别人的。
4. 出审查报告：按严重级别分级，每条给 文件:行、问题、失败场景。
5. 按长期收益最大化原则修正发现的所有级别的问题（含低级别、风格、一致性）。每个问题选长期维护成本最低的修法（根治，不绕过、不打临时补丁）；核实为误报的标明，不硬改。
6. 修完重跑相关测试与检查，全过再提交。

规则文件须提交并合入其他会话读取的分支后才对其生效。审查会话换不同模型或编辑器，有助于避开同一模型的盲区。

### 触发话术

规则已自动加载时可很短；未自动加载时在新会话开头贴一遍本流程。

- 阶段 2：「按 docs 规则修正 docs/ 下的文档，只记现状；研究、规划、待办放 planning/。」
- 阶段 3a：「审查未提交代码，出分级审查报告。」
- 阶段 3b：「按长期收益最大化原则修正报告里所有级别的问题，修完重跑测试再提交。」
