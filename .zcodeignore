# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Node
node_modules/
*.tsbuildinfo
apps/*/dist/
coverage/

# Env
.env
.env.local
deploy/compose/.env

# OpenAPI 导出产物（由脚本生成，不入库；契约快照在 services/api/tests/__snapshots__/）
openapi.json

# OS / IDE
.DS_Store
.idea/
.vscode/

# MinerU 本地解析输出
third_party/mineru/output/
# AGE 扩展源码（构建 postgres-age 镜像前经 `make age-src` 克隆，见 Makefile）
deploy/compose/postgres-age/age-src/
tmp/

# Celery beat 本地调度状态（运行时产物）
celerybeat-schedule.db
celerybeat-schedule.*.db

# 应用运行参数（含密钥；模板 config/loomvec.example.json 入库）
config/loomvec.json

# ===== ↑ 以上同步自 .gitignore（「从 .gitignore 同步」只重写以上部分）=====
.git/
.hg/
.svn/
bower_components/
jspm_packages/
site-packages/
venv/
lcov-report/
cmakefiles/
cmake-build-*/
bazel-*/
pods/
deriveddata/
storybook-static/
playwright-report/
test-results/
allure-results/
allure-report/
cdk.out/
*.dist-info/
eggs/
pip-wheel-metadata/
wheels/
# ----- ↑ 以上为 ZCode 默认排除规则（自定义规则请写在本行下方，不会被同步/恢复改动）-----
# 自定义规则写在下方（本行提示可删除）
