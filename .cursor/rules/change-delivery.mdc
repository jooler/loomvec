---
description: 改动交付流程——写代码→同步 docs→新会话审查修完→提交
alwaysApply: true
---

# 改动交付流程（铁律）

顺序固定：**写代码 → 同步 docs/ → 新会话审查并修完 → 提交**。提交只发生在最后一步。

1. **写代码**：按项目规范开发测试；改完不提交、不自审。
2. **同步 docs/**：只记当前代码事实；研究/规划/方案/待办放 `planning/`；无影响须明示「无需改 docs」。
3. **新会话审查**：在含未提交改动的工作目录开干净会话；范围 = `git status` + `git diff` + 未跟踪文件；出分级报告（严重级别、文件:行、失败场景）。
4. **同会话修完再提交**：按长期收益最大化修所有级别问题（根治、不打补丁；误报标明）；重跑测试与检查全过再提交。

全文与触发话术见仓库根目录 [AGENTS.md](../../AGENTS.md)。
