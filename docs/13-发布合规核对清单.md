# 13-发布合规核对清单（P4-QA-03）

> 上级：[11-阶段P4-运营端与企业化](./11-阶段P4-运营端与企业化.md) · 用途：进入试运行前的发布门禁，全部勾选方可发版。
>
> **核验状态说明**：标注「2026-09-13 巡检」的条目为本轮机器/实测核验（代码事实、依赖扫描、
> 临时容器迁移演练、本地全栈实测），复核人只需抽查方法；未勾选条目需商务判断或在目标
> 环境（staging/生产/CI）执行，由发布负责人完成。

## 一、MinerU 条款检查（Apache-2.0 + 附加条款，见 third_party/mineru/LICENSE.md）

- [ ] **收入/用户门槛**：确认本组织及关联方 MAU 未超过 1 亿，且月收入未超过 2000 万美元（超过任一阈值须先取得 MinerU 商业授权）；
- [ ] **在线服务标识**：产品以在线服务形式向第三方提供时，已在服务界面或公开文档显著标识"本产品使用 MinerU"（自部署内部使用不触发本条，对外发布前必查）；
- [x] 条款版本复核：LICENSE.md 随 vendored 源码更新时重新走本节（负责人：法务 + 工程）；
  （2026-09-13 巡检：在库 LICENSE.md 确认为 Apache-2.0 + 附加条款，含商业门槛（§1）与在线服务标识义务（§2）原文）
- [x] 退路确认：管线解析器可整体切换 Docling（MIT）。
  （2026-09-14 落地：`core/pipeline/parsers.py` 定义 `Parser` Protocol 与链式注册表
  （MinerU → 纯文本兜底），新增/替换解析器仅在 `build_parser_chain` 登记即可，管线步骤零改动）

## 二、License 清单盘点

| 组件 | License | 商用约束 | 核对 |
|---|---|---|---|
| MinerU（vendored） | Apache-2.0 + 附加条款 | 见第一节 | [x]（2026-09-13 核对 LICENSE.md 原文） |
| FastAPI / SQLAlchemy / pydantic / Alembic / uvicorn | MIT / BSD-3（安装元数据实测） | 无 | [x] |
| Celery | **MPL-2.0**（文件级弱 copyleft，按文件开源修改即可，商用无碍；2026-09-13 实测更正，原表"MIT/BSD 类"不准确） | 修改其源码文件须同样 MPL 开源 | [x] |
| Milvus | Apache-2.0 | 无 | [x] |
| PostgreSQL + AGE | PostgreSQL License / Apache-2.0 | 无 | [x] |
| Redis | RSALv2/SSPLv1（自托管） | 托管转售受限，自托管无 | [x] |
| RustFS | Apache-2.0 | 无 | [x] |
| Keycloak（测试 IdP） | Apache-2.0 | 仅测试环境 | [x] |
| React / Vite / Tailwind CSS / radix-ui（shadcn/ui） | MIT | 无 | [x] |
| jina 系列（若有引入） | CC-BY-NC | **禁商用，禁止引入** | [x]（2026-09-13 扫描 uv.lock / pnpm-lock.yaml 均 0 命中） |
| **igraph / python-igraph**（`loomvec-core[communities]` 可选 extra） | **GPL-3.0 系**（2026-09-13 扫描发现，原表未登记） | Leiden 社区检测加速用，未安装时自动降级 networkx（BSD-3）；worker 镜像 `uv sync --all-extras` 会将其打入生产镜像。**2026-09-14 决策：接受可选组件 GPL 并留痕**（已录入 `scripts/check_licenses.py` 白名单，随 CI 强制其余依赖合规） | [x] |
| leidenalg（同上 extra） | GPL-3.0-or-later | 同上，同一决策留痕 | [x] |

- [x] `uv.lock` / `pnpm-lock.yaml` 与上表一致（新增依赖需在此登记并核对 License）；
  （2026-09-14：全量扫描通过——白名单（igraph/leidenalg）外无 GPL/AGPL/SSPL/EUPL/CC-BY-NC 命中；
  扫描已固化为 `scripts/check_licenses.py` 并进 CI 强制，新增 Copyleft 依赖会被 CI 拒绝）
- [x] CI 依赖审计通过（`pnpm audit` / license 扫描）。
  （2026-09-14：CI 新增 `audit` job——python license 扫描 + `pnpm audit --prod` + 前端 license
  扫描；本地等效命令实测全部通过，CI 首跑随下次 push 确认）

## 三、`.env` 全量盘点

- [x] `.env.example` 与 `core/config.py` 的 Settings 字段一一对应；
  （2026-09-13 巡检：脚本双向比对 137 个 Settings 叶子字段全覆盖——补齐了此前缺失的 44 个
  进阶可运维参数；2026-09-14 修订：移除 `LOOMVEC_ALERT_WEBHOOK_URL` 占位变量——Alertmanager
  不支持配置文件内环境变量替换，critical 告警 webhook 地址直接写在 deploy/monitoring/alertmanager.yml）
- [ ] 生产环境 `LOOMVEC_AUTH__DEV_MODE=false`，`JWT_SECRET` 为强随机 ≥32 字节；
- [ ] `LOOMVEC_AI__MOCK=false`（生产禁止 mock 供方）；
- [ ] 全部密钥（AI api_key、JWT secret、storage secret、OAuth client_secret、webhook secret）经 K8s Secret / KMS 注入，代码库与镜像中无明文；
  （2026-09-13 巡检：构建侧已堵漏——根 `.dockerignore` 排除 `.env`/`deploy/compose/.env`，镜像上下文不带密钥）
- [ ] `GRAFANA_ADMIN_PASSWORD`、Keycloak 管理口令已从默认值修改（当前 compose 默认仍为 admin/admin）。

## 四、迁移与数据演练

- [x] `alembic upgrade head` 空库一路执行成功（当前 head：0005）；
  （2026-09-13 巡检：临时容器 `loomvec/postgres-age:18` 空库实测 0001→0005 全通过、AGE 扩展在位、33 张表；staging 环境复跑随发布流程执行）
- [x] `alembic downgrade -1`（0005 → 0004）演练成功（幂等回退验证）；
  （2026-09-13 巡检：同一临时容器实测 downgrade 0005→0004 后再 upgrade 回 0005，均成功）
- [ ] 备份恢复演练通过：`scripts/backup/drill.sh`（破坏性写入 → 恢复 → 行数校验）；
- [ ] PITR 前提：PG archive_mode/WAL 归档至对象存储已在生产配置（见 backup.sh 注释）。

## 五、K8s / 监控 / 安全

- [ ] `helm lint deploy/k8s/loomvec` 通过，`helm install` 一键部署成功（api/worker 多副本 + HPA + 滚动更新）；
  （2026-09-13 注：worker values 已修正为三队列全监听 `pipeline,pipeline_high,pipeline_low`——漏掉 pipeline_low 会导致合并/社区/webhook 补投任务无人消费）
- [ ] Prometheus 抓取 loomvec-api / loomvec-worker 指标正常，Grafana「LoomVec 平台总览」面板出数；
  （2026-09-13 注：指标导出已补实——api `/metrics` 含队列/死信/组件/配额 gauge，worker :9808
  多进程聚合导出 `loomvec_pipeline_jobs_total`；compose prometheus 经 host.docker.internal 抓宿主机进程）
- [ ] 告警规则（队列积压/失败率/组件宕机/配额超限）演练触发至少一次并送达接收人；
- [ ] Loki 日志可在 Grafana 检索（应用容器 JSON 日志）；
- [ ] 镜像扫描（trivy/grype）无 Critical 漏洞（本机未装扫描工具，待 CI 或发布机执行）；
- [ ] pre-commit（ruff/prettier）CI 通过；
  （2026-09-13 注：CI 实际跑 ruff check/format + eslint/tsc；prettier 仅在 pre-commit 本地钩子，CI 未含）
- [x] 全局限流生效验证（>rate_limit_per_min 触发 429）。
  （2026-09-13 巡检：本地全栈实测，匿名请求 300 次后精确触发 429，与默认 `rate_limit_per_min=300` 一致）

## 六、平台角色与开放平台验收（对应 P4 退出标准）

- [x] 角色矩阵测试全绿：`uv run pytest services/api/tests/test_admin_matrix.py`；
  （2026-09-13 巡检：46 用例全部通过）
- [ ] 全链路监管演练：建租户 → 配 OIDC 域 → 用户域内登录 → 建空间 → 上传 → 审核 → 检索；
- [ ] 企业 IdP（Keycloak realm：deploy/compose/keycloak/loomvec-realm.json）登录可用；
  （realm 文件在库已确认，登录可用性需 `--profile sso` 实测）
- [ ] 第三方应用经 OAuth 授权码流程以用户身份调用 API（权限不超用户本人）；
- [ ] Webhook 事件可订阅、投递日志可查、失败可重放；
- [ ] 审计日志可筛选、可 JSONL 导出（auditor 角色）。
  （2026-09-13 注：导出端点 `/api/v1/admin/audit-logs/export`（JSONL 附件下载）与只读角色闸门已在库并纳入矩阵测试；导出文件实际下载核验留发布演练）

## 签署

| 项 | 负责人 | 日期 |
|---|---|---|
| MinerU 条款 | | |
| 依赖 License | | |
| 密钥盘点 | | |
| 迁移/备份演练 | | |
| 监控告警 | | |
| 平台验收 | | |
